# Add group information to nodes based on 'undefined' groups.

Add group information to nodes based on 'undefined' groups.

## Usage

``` r
add_group(nodes_df)
```

## Arguments

- nodes_df:

  Data frame of nodes with columns: id, kegg_name, components.

## Value

nodes_df with updated 'group' column.
