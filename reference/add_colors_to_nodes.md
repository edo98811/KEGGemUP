# Add color palettes

Add color palettes

## Usage

``` r
add_colors_to_nodes(nodes_df)
```

## Arguments

- nodes_df:

  Data frame of nodes with 'value' and 'source' columns.

## Value

nodes_df with colored nodes based on their values.
